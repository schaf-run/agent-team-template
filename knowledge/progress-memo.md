# Progress Memo

Running log the CPO updates as work completes. Newest entries at the
top. Each entry: date, what was done, which agent(s) did it, and anything
notable for future reference.

- **2026-09-21** — Governance restructuring: renamed Manager role to CPO, added a new Manager sub-tier (Junior/Middle, `knowledge/`-only mechanical bookkeeping, capped at 1 active project-wide) per user instruction. Per-job Senior Worker cap stays at 1; independent Senior-level work should be split into separate jobs/areas (e.g. frontend vs backend) to parallelize instead. New files: `.claude/agents/manager-junior.md`, `.claude/agents/manager-middle.md`. `CLAUDE.md` updated throughout.
- **2026-09-21** — Architect delivered the full execution plan for the 3D model viewer app (React frontend, Rust backend). Saved in full to `knowledge/docs/3d-model-viewer-plan.md`. Key decisions: axum + tobj + meshopt + GLB delivery with `EXT_meshopt_compression`, LOD ladder for progressive loading, chunked S3-multipart-shaped uploads, SQLite via sqlx (runtime queries, not macros), two-ID scheme (ULID internal / CSPRNG base62 public slug). 6 jobs (J0–J5) with ~58 tasks. 15 open questions raised for the user (link-immediate-vs-withheld, retention, mobile support tier, styling, etc.) — defaults assumed, none block Phase 0. Per-job Senior-worker cap (1 active) means B0-1 and B0-2, both Senior tasks in J0, run sequentially rather than in parallel as the Architect suggested.
- B0-1 done: Senior Worker assessed the task as well-specified transcription work and recommended delegating down; accepted, re-spawned as Middle Worker, which authored and validated `contract/openapi.yaml` (OpenAPI 3.1, validated with `openapi-spec-validator`). 16 schemas defined. One inferred detail flagged for confirmation: the SSE event payload shape on `GET /models/{slug}/events` wasn't specified upstream, so the worker documented a reasonable `{status, progress, error}` shape in the spec — should be confirmed once F4-10/B5-3 (SSE consumer/producer) start.
- B0-2 done: **GO** — `tobj` peak RSS measured at ~1.02GB on a real 554MB synthetic .obj (parse 3.23s), extrapolates to ~970MB/2.9s at 500MB, well under the 2GB budget. No streaming-parser fallback (B1-7) needed. Pinned versions: axum 0.8.9, tobj 4.0.5, meshopt 0.6.2, gltf-json 1.4.1 (stale ~2.3yr but functional), sqlx 0.9.0, ulid 3.0.0, rand 0.10.3. API drift found: `rand::rngs::OsRng` no longer exists — renamed `SysRng`; `tobj::Mesh.positions`/`.normals` are `Vec<f32>` not f64. All other assumed APIs (meshopt's 6 functions, sqlx runtime queries, gltf-json GLB assembly) confirmed as-is.
- Spawned next wave in J0 (4 concurrent, within the 1 Middle + 3 Junior per-job cap): B0-5 (Middle, Cargo workspace scaffold), B0-3/B0-4/B0-8 (Junior — TS type gen, contract fixtures, guidelines.md). B0-6 (frontend scaffold) and B0-7 (obj fixtures) are Middle-level and queued behind B0-5 due to the 1-Middle-per-job cap.
- B0-8 done: `knowledge/guidelines.md` populated with 6 entries (runtime sqlx queries, no GLB gzip, single transcode pipeline for all sizes, pinned versions + API gotchas, slug secrecy, no-React-in-three/ rule).
- B0-3 done: was a PATH/shell-sourcing artifact, not a real absence — nvm had Node 20.10/20.19/24.8 installed but unsourced in the Junior's non-interactive shell. Senior Worker sourced nvm, then hit a second, unrelated bug: `contract/package.json` pinned `openapi-typescript@^8.2.0`, which doesn't exist (latest is 7.13.0) — fixed to `^7.13.0`, reran, `contract/types.ts` generated (844 lines, correct schemas/operations). Added a guideline: source nvm before declaring Node unavailable.
- B0-6 done: `frontend/` scaffolded (Vite + React 19 + TS 6, react-router-dom 7, zustand 5). dev/build/test(vitest)/lint(oxlint)/e2e(playwright) all verified passing. Routes `/` and `/m/:slug` render; `/api/*` dev-proxy to the Rust backend on :8080 verified live. Feature-folder boundary (`viewer/three/` vs `viewer/components/`) created empty per guidelines. WASM/worker setup: `vite-plugin-wasm` + `worker:{format:'es'}` + `target:'esnext'` — recorded as a guideline since it wasn't a default-obvious choice (see guidelines.md).
- B0-7 done: 9 hand-written `.obj` edge-case fixtures in `fixtures/small/` (cube, quads, n-gons, negative indices, no normals, CRLF, BOM, degenerate triangle, groups+materials), all verified to parse with `tobj` 4.0.5. Plus `fixtures/generate.rs` (standalone Cargo package, zero deps) — streams a tessellated-plane grid to an exact byte target; verified at 1MB/100MB/600MB, all within ~0.03% of target size, fastest run 3.5s for 600MB.
- **Job 0 (Contract & Scaffolding) complete** — all 8 tasks (B0-1 through B0-8) done. Paused here at user's request before starting Phase 1.
- B0-4 done: `contract/fixtures/` populated with manifest-ready.json, manifest-processing.json, manifest-failed.json — all valid, field names match the OpenAPI spec exactly, null-handling for bounds/camera/stats while processing/failed made explicit.
- B0-5 done: `backend/` Cargo workspace scaffolded (4 crates: model-core/model-transcode/model-storage as doc-stub libs, model-api as a working bin serving `/healthz`, verified via curl). `cargo build`/`clippy --all-targets` clean. All 15 pinned dependency versions resolved without conflict. Two deviations from spec, both benign: used edition 2024 (stable, toolchain is 1.98.1) instead of 2021; used tracing-subscriber "0.3" not "1" (0.3.x is still the current stable line). **Not yet exercised/verified in this task:** meshopt's native build (links a C++ lib — may need a C++ toolchain, unconfirmed) and the `rand::rngs::SysRng` rename — flag for whoever picks up B1-1/B2-x.
